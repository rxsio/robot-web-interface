<script setup>
import { defineProps } from 'vue'
const props = defineProps(['config'])
</script>
<template>
    <div>
        <p>Test window</p>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
        <code>{{ JSON.stringify(props.config) }}</code>
    </div>
</template>
